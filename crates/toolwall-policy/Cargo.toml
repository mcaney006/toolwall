[package]
name = "toolwall-policy"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
toml = "0.7"
globset = "0.4"
serde_json = "1.0"
toolwall-core = { path = "../toolwall-core" }


