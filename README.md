# toolwall
toolwall sits between AI clients and MCP servers, blocks dangerous tool calls, fingerprints tool definitions, detects poisoned tool metadata, records every decision, and lets teams replay what an agent actually did.
